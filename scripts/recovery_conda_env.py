#!/usr/bin/env python3
"""
Recovery guide for conda environment creation
"""

print("="*80)
print("RECOVERING FROM CONNECTION CLOSED DURING CONDA ENV CREATION")
print("="*80)

print("""
STEP 1: Reconnect to your server
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SSH back into your RunPod instance:
  ssh root@<your-runpod-ip>
  # OR use RunPod's web terminal

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 2: Navigate to project directory
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

cd /workspace/kernel-divergence-sad
# OR wherever you cloned the repo

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3: Initialize conda (if needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

eval "$(/root/miniconda3/bin/conda shell.bash hook)"
conda --version

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 4: Check if conda environment already exists
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Check if 'kds' environment exists:
  conda env list | grep kds

If you see "kds" in the list:
  ✅ Environment was created successfully!
  → Skip to STEP 6 (activate environment)

If you DON'T see "kds":
  ❌ Environment creation was interrupted
  → Go to STEP 5 (recreate environment)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 5: If environment doesn't exist - recreate it
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If conda env list doesn't show 'kds', recreate it:

  conda env create -f environment.yml -y

This will:
  • Download and install all dependencies
  • Take 5-10 minutes
  • Create environment named 'kds'

⚠️  If you get an error about environment already existing:
  conda env remove -n kds
  conda env create -f environment.yml -y

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 6: Activate environment
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

conda activate kds

Verify activation:
  which python
  python --version

You should see Python path in conda environment.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 7: Install missing dependency (hf_transfer)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

pip install hf_transfer

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 8: Continue with remaining steps
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

From here, continue with:
  • Step 5: Set up Hugging Face token
  • Step 6: Make scripts executable
  • Step 7: Verify GPU
  • Step 8: Run experiment in TMUX

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

QUICK COMMANDS SUMMARY:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. Initialize conda
eval "$(/root/miniconda3/bin/conda shell.bash hook)"

# 2. Check if environment exists
conda env list | grep kds

# 3a. If EXISTS - activate it
conda activate kds

# 3b. If NOT EXISTS - create it
conda env create -f environment.yml -y

# 4. Install hf_transfer
pip install hf_transfer

# 5. Verify
python --version
which python

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
""")

